import 'package:flutter/material.dart';

class IndexText extends StatelessWidget {
  const IndexText({super.key});

  @override
  Widget build(BuildContext context) {
    return const Text(
      "Hi there! I'm Gideon Iduma 🙂",
      style: TextStyle(
        color: Colors.white,
        fontSize: 24,
        fontWeight: FontWeight.bold,
      ),
    );
  }
}
